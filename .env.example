PORT=3000
NODE_ENV=production

# Token de autenticação para o ESP32 enviar dados reais (opcional)
# Se definido, o ESP32 deve enviar o header: X-Sensor-Token: <valor>
# SENSOR_TOKEN=meu-token-secreto
